# Copy to /var/www/fxprime/deploy/credentials.env on the VPS (chmod 600).
# Used by docker-deploy.sh for private Docker Hub pulls.

DOCKERHUB_USERNAME=adnanh7
DOCKERHUB_TOKEN=your-docker-hub-access-token
